{
  "name": "flow-natura-sync",
  "version": "1.0.0",
  "description": "Flow Natura - Sincronización de datos de crecimiento",
  "main": "sync.js",
  "bin": "sync.js",
  "scripts": {
    "start": "node sync.js",
    "build": "npx -y @yao-pkg/pkg . --targets node20-win-x64 --output dist/NaturaManager-Sync.exe --compress GZip"
  },
  "dependencies": {
    "puppeteer-core": "^24.0.0"
  },
  "pkg": {
    "targets": ["node20-win-x64"],
    "outputPath": "dist"
  }
}
