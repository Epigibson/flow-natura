{
  "name": "flow-natura-sync",
  "version": "1.0.0",
  "description": "Flow Natura - Sincronización de datos de crecimiento",
  "main": "sync.js",
  "bin": "sync.js",
  "scripts": {
    "start": "node sync.js",
    "build": "npx -y @yao-pkg/pkg . --targets node20-win-x64 --output dist/FlowNatura-Sync.exe --compress GZip"
  },
  "pkg": {
    "targets": ["node20-win-x64"],
    "outputPath": "dist"
  }
}
